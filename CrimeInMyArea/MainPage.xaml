<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CrimeInMyArea.MainPage"
             Title="Crime In My Area">

    <Grid RowDefinitions="Auto, Auto, *, Auto">
        <VerticalStackLayout Grid.Row="0" Spacing="10" Padding="30,20,30,10">
            <Image
                Source="dotnet_bot.png"
                SemanticProperties.Description="Cute dot net bot waving hi to you!"
                HeightRequest="100"
                HorizontalOptions="Center" />

            <Label
                Text="Crime In My Area"
                SemanticProperties.HeadingLevel="Level1"
                FontSize="28"
                HorizontalOptions="Center" />
        </VerticalStackLayout>

        <Label Grid.Row="1"
            x:Name="CrimeSummaryLabel"
            Text="Fetching crime data..."
            SemanticProperties.HeadingLevel="Level2"
            FontSize="16"
            HorizontalOptions="CenterAndExpand"
            Padding="30,0,30,10"
            LineBreakMode="WordWrap"/>

        <ScrollView Grid.Row="2" Padding="10,0,10,0" x:Name="LogScrollView">
            <CollectionView x:Name="LogCollectionView" Margin="5">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Padding="5" Margin="0,0,0,5" BorderColor="LightGray" CornerRadius="5">
                            <Label Text="{Binding .}" FontSize="12" LineBreakMode="WordWrap"/>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </ScrollView>

        <Button Grid.Row="3"
            x:Name="RefreshButton"
            Text="Refresh Crime Data"
            SemanticProperties.Hint="Refreshes the crime data for the current location"
            Clicked="OnRefreshButtonClicked"
            HorizontalOptions="Center"
            Margin="0,10,0,20" />

    </Grid>
</ContentPage>